# SYN local commands

The maintained setup, demo and test commands are in [README.md](README.md). This file no longer lists older PowerShell commands that assumed a populated database and a working Docker stack.
